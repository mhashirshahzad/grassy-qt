# TODO

## Server settings popup

- [ ] Add support for server properties not represented by the graphical editor.

why is dropdown so dependent upon the position??? also the user won't probably add memory in Mb so just tell him to add memory in GB (create start.sh is bad, on save it should do that) also does x ruin all changes??? and it shouldnt take G as input start.sh controls should be on top asking for min and max ram
